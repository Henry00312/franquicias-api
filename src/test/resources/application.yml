spring:
  application:
    name: franquicias-api-test
  webflux:
    base-path: /api
  data:
    mongodb:
      # Usa MongoDB embebido para tests
      uri: mongodb://localhost:27017/franquicias-test
      auto-index-creation: true

server:
  port: 8080

logging:
  level:
    root: WARN
    com.franquicias: DEBUG
